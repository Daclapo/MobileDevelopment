<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dm2e.davidclarkson.faunaiberica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fauna Ibérica"
        android:theme="@style/Theme.FaunaIberica">

        <activity
            android:name=".MainActivityDatosAnimal"
            android:exported="false" />

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad Carnívoros -->
        <activity
            android:name=".MainActivityCarnivoros"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Actividad Aves -->
        <activity
            android:name=".MainActivityAves"
            android:excludeFromRecents="true"
            android:exported="false" />
    </application>
</manifest>
